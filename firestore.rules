rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple rule: Allow all read and write operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
